import 'package:flutter/material.dart';

class TwobedroomPlus extends StatelessWidget {
  const TwobedroomPlus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
