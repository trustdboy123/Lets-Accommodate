import 'package:flutter/material.dart';

class ChamberHall extends StatelessWidget {
  const ChamberHall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
